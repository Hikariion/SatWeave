syntax = "proto3";
option go_package = "satweave/sat-node/executors";

enum ExecutorType {
  INVALID = 0;
  SOURCE = 1;
  KeyByInputOp = 2;
  SumOp = 3;
  Sink = 4;
}

// BaseExecutor collect all type of info
message BaseExecutor {
  oneof executor {

  }

}

message Source {

}